# K12學霸養成 — 全科複習（國語・英文・數學・自然・社會）

純靜態單頁應用（vanilla HTML/CSS/JS，無框架、無 build step），可直接以 `file://` 開啟或部署到 GitHub Pages。

**資料存放與網路請求**：練習進度預設只存在瀏覽器 localStorage。若使用者選擇用 Google 登入同步（`js/sync.js`），這份進度（含每日練習紀錄、錯題本、寫作練習內容）會上傳到自架的同步 API 並在裝置間還原；不登入就完全不會上傳。另外筆順動畫會讀取本 repo 的 `strokes/`，成語配圖讀取 `img/`。家長週報由 `tools/weekly-report.js` 在伺服器端讀同步資料後經 Telegram 發送。詳見「雲端同步」一節。

## 功能

- **年級多選**：國小1-6／國中7-9／高中10-12，可任意勾選組合（如小一–小四），附國小/國中/高中快選
- **注音／拼音切換**：右上角一鍵切換，字音/字形題的標音即時跟著換
- **單元學習**：先教後考的進度模式——選年級後題庫自動切成小單元（每單元約 14 詞條），教學卡片 → 單元測驗全對過關 → 解鎖下一單元
- **每日練習**：每天一回約 25 題（同日同年級組合題目固定），混合各類題型並依弱點自動加權；每題答完即看解說，答錯重做到全對才算完成；錯題本到期題目自動混入複習；完成紀錄供家長檢視
- **錯題間隔重考**：答錯的題目排 1→3→7 天重考排程，連續答對三關從錯題本畢業
- **寫作素材庫**（60 則）：每日一句名言佳句＋用法解說＋仿寫練習，仿寫存檔家長可見
- **弱點分析**：進度頁顯示最弱類別，每日練習自動對其加重出題
- **家長週報**：每週日 20:00 由 claudebot500 的 `tools/weekly-report.js`（systemd timer `chinese-weekly-report.timer`）讀雲端同步資料，經 Telegram bot 發送本週摘要
- **成語**（800 題）：釋義選擇、例句克漏字、**同義成語**三種題型隨機出，附常見誤用辨析；國高中含歷屆會考/學測常考成語
- **俚語諺語**（320 題）：台灣常見俚語、諺語、歇後語意義配對
- **字音辨正**（420 題）：破音字、易讀錯字，比照國語文競賽題型，以教育部審訂音為準
- **字形辨正**（400 題）：錯別字選擇 + **手寫模式**（看注音寫國字：畫布手寫 → 翻答案看**筆順動畫** → 自評，錯的進錯題本）；筆順資料見 strokes/README.md
- **閱讀測驗**（54 篇題組,持續增加中）：白話與文言，國中比照會考、高中比照學測題組風格，每題附解說
- **錯題本（保留制）**：答錯自動收集並排隔天複習；答對記「連對次數」並延後複習日（3→7→14 天），不自動移除；依時間（今天/近7天/近30天）與類別篩選、單題刪除、編輯模式勾選批刪
- **答題信心標記**：答對後可按「🤔 這題用猜的」，猜對的也列入錯題本複習
- **依序刷題**：各類別照題庫順序一題不漏刷，一批 20 題，進度自動接續
- **自創題庫**：家長提供的題庫（Word 檔傳 Telegram 由 claudebot 轉檔至 js/data/custom.js），照順序作答、進度接續、共用錯題本（類別篩選「自創題庫」）
- **字卡複習**：Leitner 三盒間隔複習（盒1每天、盒2每2天、盒3每5天），答錯的成語俚語自動入盒
- **進度**：各類答題數、正確率、每日練習日曆（家長檢視）
- **成語配圖**：部分成語附 AI 生成插圖（`img/idioms/<id>.webp`），字卡與答題解說會自動顯示
- **雲端同步**：右上角 Google 登入後進度跨裝置同步（見 js/sync.js）

題目內容以教育部頒定成語典、《國語一字多音審訂表》口徑為選材範圍，題目文字皆為原創撰寫。

## 檔案結構

```
index.html          主頁面
css/style.css       深色主題樣式（mobile-first）
js/app.js           應用邏輯（含純函式層，node 可測）
js/sync.js          Google 登入 + 雲端進度同步
js/data/idioms.js   成語題庫（含 syn 同義詞、src 歷屆出處）
js/data/slang.js    俚語諺語題庫
js/data/phonics.js  字音題庫
js/data/chars.js    字形題庫
js/data/reading.js  閱讀測驗題組
img/idioms/         成語配圖（tools/gen-idiom-images.js 生成）
tools/gen-idiom-images.js  Gemini 生圖批次腳本（本機跑，非站台程式）
test/test.js        資料完整性與題目生成測試（node test/test.js）
test/zy-check.js    注音↔拼音一致性檢查（node test/zy-check.js）
```

## 開發

無 build。改完直接重整瀏覽器。提交前跑 `node test/test.js` 和 `node test/zy-check.js`（加題最容易錯的就是注音拼音不一致，這支會抓出來）。
